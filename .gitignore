/prohibitorum
/dashboard/node_modules
/dashboard/dist
/dashboard/.vite
/.vite
/node_modules
/.data
/.dev
/.env
*.swp
.DS_Store
/smoke
# goenv artifact — Go is managed by mise (see TOOLING.md), not goenv
/.go-version
# GoReleaser build output (`mise run release` / `goreleaser build`)
/dist/

# Brainstorm visual-companion artifacts
.superpowers/

# Impeccable design-skill artifacts + installed skill copies (tooling, not source)
/.impeccable/
/.agents/
/.claude/

# Editor-local config (may contain absolute $HOME paths — keep out of history)
/.vscode/
