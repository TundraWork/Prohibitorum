/prohibitorum
/dashboard/node_modules
/dashboard/dist
/dashboard/.vite
/dashboard/.pnpm-store
/dashboard/coverage
/dashboard/*.tsbuildinfo
/dashboard/src/locales/**/*.mjs
/dashboard-old/node_modules
/dashboard-old/dist
/dashboard-old/.vite
/dashboard-old/.pnpm-store
/dashboard-old/coverage
/dashboard-old/*.tsbuildinfo
/.vite
/node_modules
/.data
/.dev
/.env
*.swp
.DS_Store
/smoke
# goenv artifact — Go is managed by mise (see TOOLING.md), not goenv
/.go-version
# GoReleaser build output (`mise run release` / `goreleaser build`)
/dist/

# Brainstorm visual-companion artifacts
.superpowers/
/.worktrees/

# Impeccable design-skill artifacts + installed skill copies (tooling, not source)
/.impeccable/
/.agents/
/.claude/

# Editor-local config (may contain absolute $HOME paths — keep out of history)
/.vscode/
/pkg/webui/dist/*
!pkg/webui/dist/.gitkeep
